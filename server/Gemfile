source 'https://rubygems.org'
ruby '2.1.0'

# Used by rvm in case there's no .ruby-gemset & ruby-version
#ruby=ruby-2.1.0
#ruby-gemset=DailySoccerServer

gem 'puma', "2.8.1"

gem 'sinatra', '1.4.4'
gem 'json', '1.8.1'

group :development do
end

group :development, :test do
end

group :test do
  gem "rack-test", "0.6.2"
  gem "rake", "10.2.1"
end

# Heroku production. pg is the PostgreSQL and rails_12factor gem, which is used by Heroku
# to serve static assets such as images and stylesheets.
group :production do
  gem 'pg', '0.15.1'
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.1.2'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
